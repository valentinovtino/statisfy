import Savory from './Savory';
import { userCleanedData } from '../../mockData';

describe('Savory', () => {
  it('should render a card', () => {
    expect(Savory).toMatchSnapshot();
  });

  it('should return clean data obj', () => {
    let 
  })

});